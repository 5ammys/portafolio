---
interface Props {
  title?: string;
  id:string;
}

const { title = "Contacto", id } = Astro.props;
---

<section id={id} class="w-full bg-gray-900 text-gray-200 py-8">
  <h2 class="text-2xl font-bold mb-6 text-center">{title}</h2>
  
  <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mx-40">
    <!-- Información de contacto -->
    
  </div>
</section>
