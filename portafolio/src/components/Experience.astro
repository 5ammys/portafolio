---
import ExperienceItem, {type JobProps} from './ExperienceItem.astro';


const EXPERIENCES = [
  {
    title: "Adox S.A - Pasante IT",
    releaseDate: "Mayo a Septiembre de 2019",
    description:
      "Creación y mejora de entornos visuales con HTML, CSS y Javascript. Creación de funcionalidades para backend en Java. Trabajo en equipo con otros pasantes y con apoyo de los profesionales del area. Participación en proyectos de producción.",
  },
]
---

<ol class="relative border-s border-gray-200 dark:border-gray-700">  
  {EXPERIENCES.map((experience: JobProps, index: number) => (
    <ExperienceItem 
      title={experience.title}
      releaseDate={experience.releaseDate}
      description={experience.description}
      latest ={index == 0 ? true : false}
    />
  ))}
</ol>
